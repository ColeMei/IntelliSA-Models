# HPC-specific requirements (Spartan)
# Install these after loading CUDA and Python modules

# Core ML libraries (with specific CUDA support)
torch>=2.0.0 --index-url https://download.pytorch.org/whl/cu121
torchvision --index-url https://download.pytorch.org/whl/cu121
torchaudio --index-url https://download.pytorch.org/whl/cu121
transformers>=4.30.0
datasets>=2.12.0
accelerate>=0.20.0
peft>=0.4.0

# Data processing
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
jsonlines>=3.1.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# HPC-specific
mpi4py  # For multi-node training if needed

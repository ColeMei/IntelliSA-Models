# HPC-specific requirements (Spartan)
# Install these after loading CUDA and Python modules

# Core ML Libraries
torch>=2.0.0
torchvision
torchaudio
transformers>=4.30.0,<4.40.0  # Pin to avoid compatibility issues
datasets>=2.12.0,<3.0.0       # Avoid v4.x which has breaking changes
accelerate>=0.20.0
peft>=0.4.0

# Data Processing
pandas>=1.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
jsonlines>=3.1.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# HPC specific
mpi4py
